type Info @model @auth(rules: [{ allow: public, operations: [read] }, { allow: private }]) {
  id: ID!
  business: String!
  email: String
  status: String
  url: String
  titleUrl: String
  thumbnail: String
  subscription: String
  stripeCustomerId: String
}